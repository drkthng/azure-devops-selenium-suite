There's a structural rule to add to style/house-rules.md. A test case verifies an expected outcome; it never instructs the user to take an action. Titles must state the behaviour being checked as an assertion about the system, not as a command. Wrong: "Do not copy RFQ basket when confirmation is cancelled." Right: "RFQ basket is not copied when quick-create confirmation is cancelled." Add this rule to the title conventions, with this example, and check whether the same action-vs-outcome confusion appears in how steps' expected results are worded — flag any cases you find, don't auto-fix them.