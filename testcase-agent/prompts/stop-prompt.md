Stop — do not encode inventory omissions into the golden files. Golden inputs must contain only what the matching step will see at runtime, i.e. the inventory as it actually is. The rule from the rules session applies: inventory gaps get fixed in the inventory tool, not worked around downstream.

Instead: (1) List the specific public properties you found missing — class, property name, what page surface/component it exposes. (2) Extend tools/build-step-inventory to capture public properties on page-object classes whose type is another page object or component: new entry kind page-object-property, fields id, class, property name, returns (the exposed type), purpose (same evidence-only rule as methods), usages. They are navigation edges — represent them so the returns-graph stays walkable. (3) Re-run the tool, report the new entry counts. (4) Only then rebuild the golden pairs against the updated inventory — inputs now include the relevant property entries where the match legitimately runs through one.