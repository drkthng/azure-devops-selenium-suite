Build a local pipeline that makes the screenshots inside our work-instruction PDFs readable for you, without model vision. Create a tools/ folder. Write a Python script tools/extract_and_ocr.py that: (1) for each PDF in knowledge/work-instructions/, extracts all embedded images using PyMuPDF, skipping decorative ones (smaller than ~200 px wide or ~10 KB — logos, icons, bullets), saving them as knowledge/screenshot-captions/_images/<pdf-stem>/p<page>_i<index>.png; (2) runs local OCR on each image — try the winocr package first (wraps the built-in Windows OCR engine, fully offline; OCR in German and English), fall back to rapidocr-onnxruntime, then Tesseract, if it won't install; (3) writes the raw OCR text to knowledge/screenshot-captions/<pdf-stem>.raw.md, one section per image with its page reference. Install what you need via pip — assume no admin rights; if pip can't reach PyPI, stop and tell me. Run it on all three PDFs and report: images extracted, skipped, OCR coverage. Then, as a second step, read each .raw.md alongside the corresponding PDF's text and produce the clean caption file knowledge/screenshot-captions/<pdf-stem>.md in our usual format — screen/dialog name, field labels, button labels, states, per image with page reference. OCR output is noisy: where a label is garbled or ambiguous, write [OCR UNCLEAR: <what's unreadable>] — never repair it by guessing a plausible label. Finish with the list of all [OCR UNCLEAR] spots.