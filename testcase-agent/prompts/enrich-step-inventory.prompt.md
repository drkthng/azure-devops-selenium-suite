Read step-inventory.json. For each id in needs-description, open only that method's source, read its body, and write a one-line purpose describing what it does in business/UI terms (what a tester would say, not what the code does mechanically). Write the completed purposes back into the JSON and re-render the MD. Where a method's intent is genuinely unclear from its body, set purpose to "UNCLEAR: <why>" instead of guessing.