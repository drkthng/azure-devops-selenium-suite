Your task is to create test cases from the work instructions in knowledge/work-instructions/. First read the style files, schema, and glossary so you follow our conventions. These PDFs contain screenshots you cannot read reliably from the file — only the surrounding text.
Work through each instruction and generate the test cases it implies, following the schema, writing them to out/. Where the text gives you everything you need, write the complete test case. Where a step depends on something only an image would show — exact field names, a button's precise label, a screen you can't name — do not guess: write the test case as far as the text allows and mark the unresolved part with [NEEDS SCREENSHOT: <what you'd need to see>] at that step.
When you're done, give me a short list of the [NEEDS SCREENSHOT] points so I can send the relevant images. I'll then send them and you complete those cases.