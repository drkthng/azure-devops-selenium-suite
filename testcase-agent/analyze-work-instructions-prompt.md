Read the work-instruction PDFs in knowledge/work-instructions/. These PDFs contain screenshots that you cannot read reliably from the file — only the surrounding text. For each instruction, work through the procedure from the text and produce a list of points where the text alone is not enough to write a precise test step: places that reference a form, dialog, screen, or UI element whose exact field names, button labels, or layout are not spelled out in the words. For each, tell me which instruction and which step, and what specifically you'd need to see. Do not guess the missing labels. I will then send you the relevant screenshots so you can fill those gaps.